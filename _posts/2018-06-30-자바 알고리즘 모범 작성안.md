---
layout: post
title: 자바 알고리즘 모범 작성안
excerpt: "객체지향 언어에 맞는 알고리즘 코딩 방법"
tags: [Java, Algorithm]
categories: [Algorithm]
link:
comments: true
pinned: true
image:
  feature: algorithms.png
---

객체 지향에 충실하다는 느낌 그대로이다. 앞으로 이렇게 연습해야겠다.

선영님 백준 9461번


~~~java
import java.io.*;
import java.util.*;

class Main {
	public static void main(String[] args) {
		new Wave().start();
	}
}
class Wave{
	long[] arr;

	void start() {
		try{
			arr=new long[101];
			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));
			int T=Integer.parseInt(br.readLine());

			init();
			int N;
			for(int i=0; i<T; i++){
				N=Integer.parseInt(br.readLine());
				getResult(N);
			}
		}catch(IOException ie) {}
	}
	void init(){
		arr[1]=1;
		arr[2]=1;
		arr[3]=1;
		arr[4]=2;
	}
	void getResult(int N){
		if(arr[N]==0) setArr(N);
			
		System.out.println(Long.toString(arr[N]));
	}
	void setArr(int N){
		if(N==0) return ;

		if(arr[N-1]==0) setArr(N-1);
		arr[N]=arr[N-1]+arr[N-5];
	}
}
~~~



선영님 백준 1676번

~~~java
import java.io.*;

class Main{
	public static void main(String[] args) {
		new Factori().showResult();
	}
}
class Factori {
	int n;
	
	Factori(){
		try {
			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));
			n=Integer.parseInt(br.readLine());
		}catch(IOException ie) {}
	}
	int countZero() {
		int count=0;
		
		for(int i=n; i>0; i--) {
			if(i%5==0) count+=getFiveNum(i);
		}
		
		return count;
	}
	int getFiveNum(int num) {
		int re=0;
		
		while(num>=5) {
			if(num%5!=0) break;
			re++;
			num/=5;
		}

		return re;
	}
	void showResult() {
		System.out.println(Integer.toString(countZero()));
	}
}
~~~



선영님 백준 11051번

~~~java
import java.io.*;

class Main {
	public static void main(String[] args)  {
		new Binomial2().setDP();
	}
}
class Binomial2{
	int n,k;
	int[][] arr;
	Binomial2() {
		try{
			BufferedReader br=new BufferedReader(new InputStreamReader(System.in));
			String in=br.readLine();
			int idx=in.indexOf(" ");
			n=Integer.parseInt(in.substring(0,idx));
			k=Integer.parseInt(in.substring(idx+1));

			arr=new int[n+1][n+1];
		}catch(IOException ie){}
	}
	void setDP(){
		arr[1][0]=1;
		arr[1][1]=1;

		for(int i=2; i<=n; i++){
			for(int j=0; j<=i; j++) {
				if(j==0) arr[i][j]=arr[i-1][j];
				else arr[i][j]=(arr[i-1][j]%10007+arr[i-1][j-1]%10007)%10007;

				if(i==n && j==k) break;
			}
		}
		showResult();
	}
	void showResult(){
		System.out.println(Integer.toString(arr[n][k]));
	}
}
~~~

